name: Deploy Worker to Aliyun ECS

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 允许手动触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.ALIYUN_SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.ALIYUN_SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        env:
          PROJECT_PATH: ${{ secrets.ALIYUN_PROJECT_PATH }}
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_SERVER_HOST }} << EOF
            set -e
            
            # 进入项目目录
            cd ${PROJECT_PATH}/worker || exit 1
            
            # 拉取最新代码
            echo "📥 拉取最新代码..."
            git fetch origin
            git reset --hard origin/main
            
            # 如果使用 Docker Compose（worker 通常和 backend 一起部署）
            if [ -f "\${PROJECT_PATH}/backend/deploy/docker-compose.aliyun.yml" ]; then
              echo "🐳 重新构建并重启 Worker 容器..."
              cd \${PROJECT_PATH}/backend
              docker-compose -f deploy/docker-compose.aliyun.yml up -d --build worker
              
              echo "✅ 等待 Worker 启动..."
              sleep 5
              
              # 检查 Worker 状态
              docker-compose -f deploy/docker-compose.aliyun.yml ps worker
            else
              echo "⚠️  未找到 docker-compose.aliyun.yml，跳过 Docker 部署"
            fi
            
            echo "🎉 Worker 部署完成！"
          EOF

